_analysis = 
  #數值分析 
  _init : ->

  _lsm-init: (arr,number) -> 
    zigmax = 0;
    zigmax2 = 0;
    zigmay = 0;
    zigmay2 = 0;

    for i in arr
      zigmax += i.x

  lsm : (arr,number)->
    #least sequare method
    #最小平方法
    m = number -2

Kakulus.prototype.analysis = _analysis 